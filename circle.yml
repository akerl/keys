machine:
  environment:
    BUCKET: akerl-keys
test:
  override:
  - find -L all -type f | xargs -n1 ssh-keygen -l -f
  - ./build.sh
deployment:
  release:
    branch: master
    commands:
    - aws s3 sync --delete _build/ s3://$BUCKET/
